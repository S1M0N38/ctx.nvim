-----------------------------------------
-- config.lua
-----------------------------------------
local config = {
  version = "0.1.0",
  debug = true,
  settings = {
    maxUsers = 100,
    timeout = 30,
    retries = 3,
  },
}

return config
